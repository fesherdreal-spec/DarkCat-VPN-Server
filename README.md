# 🐾 DarkCat VPN Server Panel

Простой и безопасный api для управления пользователями VPN. Написан на Flask, использует SQLAlchemy для работы с базой данных и Redis для сессий.

## ✨ Основные возможности

-   **Управление пользователями**: Создание, удаление, просмотр списка пользователей.
-   **Управление конфигурациями**: Назначение и обновление конфигурационных ссылок для пользователей.
-   **Управление сроком действия**: Установка срока действия конфигурации для каждого пользователя.
-   **Безопасность по умолчанию**:
    -   Защита от подбора паролей (rate limiting) на эндпоинтах входа и регистрации.
    -   Защита админ-панели по IP-адресу.
    -   Безопасное хранение паролей (PBKDF2-SHA256).

## ⚙️ Конфигурация

Вся конфигурация сервера производится через переменные окружения. Создайте файл `.env` в корне проекта, скопировав `.env.example`.

```bash
cp .env.example .env
```

**Обязательно заполните `.env` своими значениями!**

### Ключевые переменные окружения:

-   `SECRET_KEY`: **КРИТИЧЕСКИ ВАЖНО.** Секретный ключ для подписи сессий. Должен быть длинной случайной строкой (минимум 32 байта).
    *Для генерации используйте:* `python -c 'import secrets; print(secrets.token_hex(32))'`
-   `DB_TYPE`: Тип используемой базы данных. Поддерживаются `postgres`, `mysql`, `sqlite`. **SQLite не рекомендуется для продакшена.**
-   `POSTGRES_*` / `MYSQL_*`: Настройки подключения к вашей базе данных.
-   `REDIS_HOST` / `REDIS_PASSWORD`: Настройки подключения к Redis.
-   `ROOT_ADMIN_USER`: Имя главного администратора. Этот пользователь создаётся при первом запуске `init-db` и не может быть удалён или изменён другими администраторами через API.
-   `ADMIN_IPS`: Список IP-адресов через запятую, с которых разрешён доступ к административным функциям.
-   `ALLOW_REGISTRATION`: Установите `True`, чтобы разрешить открытую регистрацию пользователей. **В продакшене рекомендуется `False`.**

## 🚀 Запуск

### 1. Локальный запуск для разработки

**Требования**: Python 3.8+, `pip`, `virtualenv`.

1.  **Клонируйте репозиторий:**
    ```bash
    git clone https://github.com/fesherdreal-spec/DarkCat-VPN-Server.git
    cd DarkCat-VPN-Server
    ```

2.  **Создайте и активируйте виртуальное окружение:**
    ```bash
    python -m venv .venv
    source .venv/bin/activate
    # Для Windows: .venv\Scripts\activate
    ```

3.  **Установите зависимости:**
    ```bash
    pip install -r requirements.txt
    ```

4.  **Создайте и настройте `.env` файл** (см. раздел "Конфигурация").

5.  **Инициализируйте базу данных:**
    ```bash
    source .venv/bin/activate
    export FLASK_APP=app.py 
    ```
    *Эта команда создаст таблицы и root-администратора.*
    ```bash
    flask init-db
    ```
    *Сохраните временный пароль, который будет выведен в консоль!*

6.  **Запустите dev-сервер:**
    ```bash
    flask run
    # или
    # python app.py
    ```

### 2. Запуск в продакшене

**НИКОГДА НЕ ИСПОЛЬЗУЙТЕ `flask run` ИЛИ `python app.py` В ПРОДАКШЕНЕ.**
Используйте полноценный WSGI-сервер, например, Gunicorn, и запускайте его за реверс-прокси (Nginx).

**Пример запуска через Gunicorn:**

```bash
gunicorn --workers 4 --bind 0.0.0.0:5000 app:app
```

-   `--workers 4`: Количество рабочих процессов. Обычно `(2 * кол-во ядер CPU) + 1`.
-   `--bind 0.0.0.0:5000`: Сервер будет слушать порт 5000 на всех интерфейсах. Nginx должен проксировать запросы на этот порт.

Рекомендуется настроить запуск Gunicorn как systemd-сервис для автоматического перезапуска.

## 📋 API Эндпоинты

-   `POST /login`: Вход в систему.
-   `POST /logout`: Выход из системы.
-   `POST /register`: Регистрация нового пользователя (если разрешена).
-   `POST /heartbeat`: Эндпоинт для клиента, чтобы сообщить, что он онлайн.
-   `GET /admin/users`: (Admin) Получить список всех пользователей с пагинацией.
-   `POST /admin/update_config`: (Admin) Обновить конфигурацию пользователя.
-   `POST /admin/delete_user`: (Admin) Удалить пользователя.
-   `POST /admin/reset_password`: (Admin) Сбросить пароль пользователя.


## 📝 TODO`s

- Реализовать запуск через докер. Написать Dockerfile и docker-compose.yml.
- Сделать удобную и отзывчивую веб-панель.
